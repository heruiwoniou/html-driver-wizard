svg.loading {
  display: none;

  path, rect {
    fill: white;
  }
}

.chart {
  svg.loading {
    display: block;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }
}